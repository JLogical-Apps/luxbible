# Lux

Shared Bible models, application infrastructure, behavior widgets, and general-purpose utilities for Lux apps.

Shared localization is available through `package:lux/i18n.dart` and generated from the files in `lib/i18n`.

Import the public library with:

```dart
import 'package:lux/lux.dart';
```
