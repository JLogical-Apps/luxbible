# Feature Specification

Lux Memory currently opens to a read-only preview of Genesis 1 in the BSB. This validates reuse of the shared Bible models, bundled content, providers, and rendering UI while the memorization experience is developed.

The ideas being considered are documented in [`roadmap.md`](roadmap.md). Move behavior into this file only after it exists in the working tree and is intended for an upcoming release.
