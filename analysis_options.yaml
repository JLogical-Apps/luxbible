include: package:flutter_lints/flutter.yaml

analyzer:
  exclude: ["**.freezed.dart", "**.g.dart"]
  plugins:
    - custom_lint

linter:
  rules:
    - always_use_package_imports

formatter:
  page_width: 90
